{
  "name": "recon-x",
  "version": "1.0.0",
  "private": true,
  "description": "RECON-X: UAV Single-Pass 3D Photogrammetry & Digital Twin Reconstruction Platform",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev"
  }
}
